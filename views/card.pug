extends layout.pug

block content
    section#content
        h2= text
        if hint
            p
                i Hint:#{hint}
        a(href=`${id}?side=${otherside}`)= `${sideToShow}`
        br
        br
        a(href='/cards') Next card